--- a/src/framac.py
+++ b/src/framac.py
@@ -4,13 +4,14 @@
 import os, io, sys, time
 import openai
 import logging
+import signal
 import subprocess
 import datetime
 from typing import List

 Check_STDOUT = 1                    # Set 1 if you want to record std result
 Check_STDERR = 1                    # Set 1 if you want to record err result
-SubprocessTimeout = 500             # Set the timeout of subprocess
+SubprocessTimeout = int(os.environ.get("AUTOSPEC_FRAMAC_WALL_TIMEOUT", "60"))


 # create subprocess according to the value of Check_STDOUT and Check_STDERR
@@ -18,13 +19,13 @@
     # Create the FRAMAC command
     # Check_STDOUT and Check_STDERR are used to check the standard output and error of the FRAMAC subprocess
     if (Check_STDOUT == 1 and Check_STDERR == 1):
-        process = subprocess.Popen(FRAMAC_Command, close_fds=True, preexec_fn=os.setpgrp, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
+        process = subprocess.Popen(FRAMAC_Command, close_fds=True, start_new_session=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
     elif (Check_STDOUT == 1 and Check_STDERR == 0):
-        process = subprocess.Popen(FRAMAC_Command, close_fds=True, preexec_fn=os.setpgrp, stdout=subprocess.PIPE)
+        process = subprocess.Popen(FRAMAC_Command, close_fds=True, start_new_session=True, stdout=subprocess.PIPE)
     elif (Check_STDOUT == 0 and Check_STDERR == 1):
-        process = subprocess.Popen(FRAMAC_Command, close_fds=True, preexec_fn=os.setpgrp, stderr=subprocess.PIPE)
+        process = subprocess.Popen(FRAMAC_Command, close_fds=True, start_new_session=True, stderr=subprocess.PIPE)
     else:
-        process = subprocess.Popen(FRAMAC_Command, close_fds=True, preexec_fn=os.setpgrp)
+        process = subprocess.Popen(FRAMAC_Command, close_fds=True, start_new_session=True)
     return process


@@ -102,11 +103,23 @@
             process.communicate(timeout=SubprocessTimeout)

     except subprocess.TimeoutExpired:
-        # process.kill()
-        # process.terminate()
-        subprocess.check_call(["sudo", "kill", str(process.pid)])
-        #os.waitpid(process.pid, 0)
-        logging.error("Timeout for subprocess when running frama-c on" + ' '.join(os.path.join(Output_folder, gfile)))
+        output_result_type = "Timeout"
+        try:
+            os.killpg(process.pid, signal.SIGTERM)
+        except ProcessLookupError:
+            pass
+        try:
+            process.communicate(timeout=5)
+        except subprocess.TimeoutExpired:
+            try:
+                os.killpg(process.pid, signal.SIGKILL)
+            except ProcessLookupError:
+                pass
+            process.communicate()
+        logging.error(
+            "Timeout for subprocess when running frama-c on "
+            + os.path.join(Output_folder, gfile)
+        )

     except Exception as e:
         print("\033[34m\tUnknown Exception" + "\033[0m")
--- a/src/llmveri.py
+++ b/src/llmveri.py
@@ -2,6 +2,7 @@
 # -*- coding: UTF-8 -*-

 import os, sys, re
+import hashlib
 import logging
 import pickle
 import copy
@@ -535,7 +536,19 @@
         max_pass_goal = 0
         max_all_goal = 0
         best_target_file = ""
-        while 1:
+        framac_attempt = 0
+        max_framac_attempts = int(
+            os.environ.get("AUTOSPEC_FRAMAC_MAX_ATTEMPTS", "16")
+        )
+        seen_framac_states = set()
+        while True:
+            if framac_attempt >= max_framac_attempts:
+                logging.warning(
+                    "Stopping Frama-C retries after %d attempts for %s",
+                    max_framac_attempts,
+                    GPT_File,
+                )
+                break
             # input("Press Enter to continue...")
             if GPT_Task == 1 or GPT_Task == 3:
                 target_file = merged_file
@@ -545,6 +558,27 @@
                 else:
                     target_file = merged_file

+            target_path = os.path.join(Output_folder, target_file)
+            try:
+                with open(target_path, "rb") as target_handle:
+                    target_digest = hashlib.sha256(target_handle.read()).hexdigest()
+            except OSError:
+                target_digest = "missing"
+            framac_state = (
+                target_file,
+                target_digest,
+                tuple(generated_acsl_spec_list),
+                ci,
+            )
+            if framac_state in seen_framac_states:
+                logging.warning(
+                    "Stopping Frama-C retries because no progress was made for %s",
+                    target_path,
+                )
+                break
+            seen_framac_states.add(framac_state)
+            framac_attempt += 1
+
             output_result_type, output_std_file_name, output_err_file_name, solve_time = run_framac_with_wp(Output_folder, target_file)
             total_solve_time = solve_time + total_solve_time
             if output_result_type == "Invalid":

--- a/utils/gptcore.py
+++ b/utils/gptcore.py
@@ -1,7 +1,7 @@
 #!/usr/bin/env python3
 # -*- coding: UTF-8 -*-
 
-import os, sys, time
+import os, sys, time, json
 import openai
 from openai import OpenAI
 import logging
@@ -134,6 +134,8 @@
                 create_kwargs["max_tokens"] = maxTokens
         if stop_str is not None:
             create_kwargs["stop"] = stop_str
+        if stream_out:
+            create_kwargs["stream_options"] = {"include_usage": True}
 
         # 重试 3 次
         for i in range(3):
@@ -160,12 +162,16 @@
         if stream_out:
             # create variables to collect the stream of chunks
             collected_messages = []
+            response_usage = None
             for _ in range(n_choices):
                 collected_messages.append([])
 
             # iterate through the stream of events
             print(f"\U0001f47D: ", end = '')
             for chunk in response:
+                chunk_usage = getattr(chunk, "usage", None)
+                if chunk_usage is not None:
+                    response_usage = chunk_usage
                 # 新版 SDK: chunk.choices[0].delta.content 可能为 None
                 for choice in chunk.choices:
                     delta_content = getattr(choice.delta, "content", None)
@@ -179,11 +185,26 @@
             for each_collected_message in collected_messages:
                 full_reply_content_list.append(''.join(each_collected_message))
             
-            # 估算 tokens 用量
-            tmp_cvlist = self.conversation_list.copy()
-            for each_reply_content in full_reply_content_list:
-                tmp_cvlist.append({'role':'assistant', 'content':each_reply_content})
-            tokens_usage = num_tokens_from_messages(tmp_cvlist, model) - n_choices*5
+            if response_usage is not None:
+                prompt_tokens = getattr(response_usage, "prompt_tokens", None)
+                completion_tokens = getattr(response_usage, "completion_tokens", None)
+                total_tokens = getattr(response_usage, "total_tokens", None)
+                details = getattr(response_usage, "completion_tokens_details", None)
+                reasoning_tokens = getattr(details, "reasoning_tokens", None) if details else None
+                usage_record = {
+                    "prompt_tokens": prompt_tokens,
+                    "completion_tokens": completion_tokens,
+                    "reasoning_tokens": reasoning_tokens,
+                    "total_tokens": total_tokens,
+                }
+                print("AUTOSPEC_API_USAGE=" + json.dumps(usage_record, sort_keys=True))
+                tokens_usage = total_tokens
+            else:
+                # Fall back only when an OpenAI-compatible endpoint omits usage.
+                tmp_cvlist = self.conversation_list.copy()
+                for each_reply_content in full_reply_content_list:
+                    tmp_cvlist.append({'role':'assistant', 'content':each_reply_content})
+                tokens_usage = num_tokens_from_messages(tmp_cvlist, model) - n_choices*5
 
         # don't use stream of chunks
         else:

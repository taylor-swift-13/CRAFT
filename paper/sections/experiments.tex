%% ============================================================
\section{Experiments}\label{sec:experiments}
%% ============================================================

We evaluate three questions in the order a reader encounters the system:
(1) how effective and costly is target-hidden inference,
(2) whether reinforcement learning improves that inference procedure, and
(3) why candidate aggregation and execution-based rewards are useful.

\subsection{Experimental Setup}
\label{sec:exp:setup}

\paragraph{Benchmarks.}
The evaluation contains 832 integer C programs.  The \emph{Core-366} subset
contains 316 linear programs from Code2Inv, SyGuS, and SV-COMP and 50
nonlinear programs from LIPuS.  The remaining 466 programs are the integer
fragment of Loopy's original 469-program corpus; three floating-point programs
are excluded because they fall outside our scalar-integer predicate language.
Every method uses the same fixed manifest, and unsupported inputs remain
failures in the common denominator.

\paragraph{Target-hidden evaluation.}
Before any model-facing step, we remove ACSL \texttt{assert} and
\texttt{ensures} predicates, executable assertion calls, and conventional
error-label names.  Preconditions and executable loop semantics remain
visible.  After a method fixes its invariants, they are inserted into the
untouched original program.  A task succeeds only if the source parses,
Frama-C/WP proves establishment and preservation for every retained invariant,
and all restored target goals succeed.  Failures, timeouts, malformed outputs,
and unsupported programs count as zero.

\paragraph{Verifier, models, and baselines.}
All final checks use Frama-C 32.1 (Germanium), WP, and Z3 4.15.4.
The main inference comparison fixes \texttt{gpt-5-nano} across all
\sam{} variants, so differences among those rows come only from inference.
We compare with the native target-hidden pipelines of \textsc{AutoSpec},
\textsc{SESpec}, and \textsc{Clause2Inv}, and with Daikon 5.8.24
~\citep{wen2024enchanting,yang2026sespec,cao2025clause2inv,daikon}.
\textsc{Clause2Inv} supports only the 366 programs with transition
verification conditions; its remaining inputs still count as failures.

The model-scale analysis samples 100 target-hidden responses per task from the
official Qwen3-4B, Qwen3-8B, Qwen3-14B, and Qwen3-30B-A3B checkpoints
~\citep{yang2025qwen3}, at temperature 1.0 with thinking disabled.
The reinforcement-learning comparison uses Qwen3-8B and the corresponding
8B-RL-Zero checkpoint under the same 100-response protocol.

\paragraph{Metrics.}
For a program with \(c\) independently successful responses among \(n=100\),
direct pass@\(k\) uses the unbiased estimator
\(1-\binom{n-c}{k}/\binom{n}{k}\).
Combine@\(k\) instead unions the clauses from the first \(k\) responses,
applies Houdini, and tests the resulting invariant set against the restored
target.  We summarize response efficiency with
\[
  k_{95}=\min\{k:C_\pi(k)\geq0.95\,C_\pi(100)\},
\]
where \(C_\pi(k)\) is combine@\(k\) for policy \(\pi\).

\subsection{RQ1: How Effective Is Target-Hidden Inference?}
\label{sec:exp:e2e}

\textsc{Naive} uses one response and a minimal prompt.
\textsc{R1-NoH} uses the \sam{} prompt but no Houdini filtering;
\textsc{R1-H} adds Houdini; and \textsc{R5-H}/\textsc{R10-H} combine five or
ten target-hidden responses before the same Houdini and final proof.
Retries and repair are disabled in this comparison.

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrrrr}
\toprule
Method & Linear / 316 & NLA / 50 & Loopy / 466 & All / 832
  & Time (s) & Exact tokens & Est. tokens \\
\midrule
\textsc{AutoSpec} & 203 & 4 & 302 & 509 (61.18\%) & 64.63 & --- & 2,031.54 \\
\textsc{Clause2Inv} & 63 & 1 & 0 & 64 (7.69\%) & 29.04 & --- & 385.80 \\
\textsc{Daikon} & 73 & 0 & 101 & 174 (20.91\%) & 4.89 & 0 & --- \\
\textsc{SESpec} & 180 & 3 & 231 & 414 (49.76\%) & 117.31 & 44,807.05 & --- \\
\midrule
\textsc{Naive} & 154 & 4 & 221 & 379 (45.55\%) & 28.87 & 4,493.37 & --- \\
\textsc{R1-NoH} & 193 & 7 & 252 & 452 (54.33\%) & 53.52 & 5,849.64 & 630.14 \\
\textsc{R1-H} & 203 & 7 & 294 & 504 (60.58\%) & 66.60 & 6,928.30 & --- \\
\rowcolor{algobg}
\sam{} (\textsc{R5-H}) & 250 & 7 & 365 & 622 (74.76\%) & 284.81 & 33,404.56 & --- \\
\textsc{R10-H} & 255 & 7 & 376 & 638 (76.68\%) & 536.81 & 68,953.35 & --- \\
\bottomrule
\end{tabular}}
\caption{Target-hidden verification results and mean generation cost per task.
Exact and estimated token averages are reported separately.  R1-NoH's time
and exact-token means cover Loopy-466, while its estimate covers Core-366.
Unsupported Clause2Inv inputs remain failures in the 832-program denominator.}
\label{tab:inference-comparison}
\end{table}

Table~\ref{tab:inference-comparison} shows that five-response inference
verifies 622 programs, 113 more than \textsc{AutoSpec}, the strongest external
baseline.  The controlled rows isolate the contributions of prompting,
Houdini, and response aggregation: 379/832 for \textsc{Naive}, 452/832 for
\textsc{R1-NoH}, 504/832 for \textsc{R1-H}, and 622/832 for
\textsc{R5-H}.

The improvement has a clear cost.  \textsc{R5-H} averages 33,404.56 exact
tokens and 284.81 seconds per task.  Increasing the budget to ten responses
adds only 16 proofs while raising the averages to 68,953.35 tokens and
536.81 seconds.  This diminishing return motivates improving the generator
rather than scaling the response budget indefinitely.

\subsection{RQ2: Does Reinforcement Learning Improve Inference?}
\label{sec:exp:rlzero}

We compare Qwen3-8B with 8B-RL-Zero while holding the candidate union,
Houdini filter, target-hiding protocol, and response budget fixed.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/qwen_rlzero_probe.pdf}
  \caption{Reinforcement learning improves combined inference even though it
  does not improve the chance that one response proves the target.  With ten
  responses, 8B-RL-Zero exceeds the base model's combine@100 result.}
  \label{fig:qwen-rlzero}
\end{figure}

Figure~\ref{fig:qwen-rlzero} shows different trends for complete responses and
complementary clauses.  RL-Zero lowers direct pass@1 from 9.30\% to 4.88\%
and leaves direct pass@100 nearly unchanged (33.29\% versus 32.93\%).
Under the deployed combination procedure, however, combine@10 rises from
52.40\% to 56.97\%, and combine@100 rises from 55.41\% to 58.77\%.
Ten RL-Zero responses outperform 100 base-model responses, while
\(k_{95}\) falls from 30 to 10.

The trained policy therefore supplies useful complementary clauses with fewer
generations.  This is the behavior encouraged by the group-relative reward:
each response can contribute clauses that survive Houdini and cover behavior
missed by its peers, even if the response is not a complete proof by itself.

\subsection{RQ3: Why Are Aggregation and Execution-Based Rewards Needed?}
\label{sec:exp:probe}

\paragraph{Candidate aggregation.}
Inductiveness by itself cannot distinguish a useful invariant from
\texttt{true}, and a binary whole-response reward discards useful clauses when
another clause in the same response fails.  Reinforcement learning with
verifiable rewards can also concentrate probability on already rewarded paths
while reducing large-\(k\) exploration~\citep{yue2025rlcapacity}.
This matters here because different responses often contain complementary
parts of a proof.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/qwen_official_probe.pdf}
  \caption{Direct pass@\(k\) measures complete responses, whereas
  combine@\(k\) measures whether the accumulated clauses contain a sufficient
  Houdini-filtered subset.  For all four official Qwen3 checkpoints, the
  combine curves change by at most 0.60 percentage points after \(k=30\).}
  \label{fig:qwen-probe}
\end{figure}

Figure~\ref{fig:qwen-probe} shows that partial responses contain substantial
value.  Direct pass@100 is 15.62\% for Qwen3-4B, 33.29\% for 8B, and
39.90\% for 14B.  Combining clauses raises 4B to 47.84\% and 14B to
58.05\%.  Houdini can recover compatible clauses from imperfect responses, but
it cannot invent a relation that the model never emits.

The curves also saturate early: no checkpoint gains more than 0.60 percentage
points between combine@30 and combine@100.  A useful reward should therefore
increase both the attainable verification rate and the rate at which new
responses contribute previously missing clauses.

\paragraph{Reward and negative-example checks.}
Across ten hand-written specifications, every gold invariant survives the
verifier filter and rejects at least 99.6\% of the intended negative examples,
with no ordering violations in the reward sanity checks.  A larger audit on
Core-366 compares 486,302 generated negative states against an expanded set of
program executions and finds no hard pair-level collision.  These checks do not
prove that every synthetic negative is globally unreachable, but they support
the conservative construction used for training.
